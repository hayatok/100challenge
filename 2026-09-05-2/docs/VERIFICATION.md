# 検証記録 — 2026-09-06

環境: macOS / Apple M5 / Godot 4.7.2 stable / Compatibility。現行の成果物は2D版の1ステージ。旧3D版の検証結果は流用しない。

## 自動検証

`npm run check`成功。6枚のPNGシグネチャ・寸法・ImageGen出自・正本SHA-256、3Dノード/モデルの非混入、Godot import、48項目のコアテスト、Web release出力を確認。`npm run build:mac`も成功。独立したJS lint/TypeScriptコマンドは設けていない。GDScriptはGodot importで解析し、`git diff --check`で差分の空白を検査。

主要なテストは有限線分交差、古い尾の除外、直近線の除外、境界点、経験値の二重加算防止、停止、連続強化、候補の重複防止、連鎖の範囲、回復上限と復活防止、ボスのシールド/ダメージ上限、HPゼロ優先、制限時間、300体上限、seed再現、保存値の型/非有限値。

以下は固定dtで移動入力と三択強化を再生した通常ルールの結果。HP・敵・ボスの数値の差し替え、無敵、早送りしたゲーム時刻は使っていない。人による完走とは区別する。

| seed | 結果 | 経過秒 | 残りHP | 囲み | 撃破 |
|---|---|---:|---:|---:|---:|
| 101 | 勝利 | 164.30 | 85 | 40 | 509 |
| 202 | 勝利 | 157.48 | 55 | 38 | 474 |
| 303 | 勝利 | 157.48 | 79 | 38 | 472 |
| 20260906 | 勝利 | 159.12 | 60 | 38 | 479 |

途中で連鎖が範囲外へ波及する不具合を見つけ、距離条件内にダメージ処理を戻して回帰テストを追加した。不具合修正前の勝利結果は採用していない。接触4ダメージ/無敵1.2秒、5体ごと3回復（最大9）、HP45未満の回復候補保証が最終設定。

## 画面・操作

Web通常版でタイトル→導入→戦闘、クリック移動、撤収・記録保存を操作。Web debug版で通常入力再生による最初の囲み（7体同時回収）、陣地生成、11体撃破後のレベルアップ停止を確認。1キーの強化、Esc停止・再開、効果音ON/OFF、勝利画面と再挑戦を確認。勝利画面を直接出すQAは見た目の確認に限り、勝利可能性の根拠は上記通常テストとする。

ブラウザ幅1440×900（タイトル/導入）、1024×768（戦闘/撤収）、768×432（三択/停止）、375×812（縦画面案内と開始ボタン）をスクリーンショットで確認。最終的に画面幅の一時指定を解除。Webプレビューはrelease出力の通常タイトルで残す。小さい縦画面で強行開始した場合は文字も縮小されるため、横向き・広い画面を推奨する。

読み込み進捗、空の最高記録、撤収、勝利を確認。`/qa-load-error/`でJS読み込み404を意図的に起こしてエラー案内と再試行ボタンを確認。通常起動のWebコンソールにエラーはなかった。HTML/JS/WASM/PCKはHTTP 200、WASMはapplication/wasmで配信。

Godotの実描画を1280×720で取得し、背景、主人公、囲み・陣地、敵3種、ボス/端子、HUDを確認。透過縁、経験値バーの太さ、会話/結果への戦闘キャラの残り、ボスと説明の重なりを修正。

## ネイティブ出力

`builds/macos/LOOP EATER.app`。Mach-O universalのx86_64/arm64を確認。書き出した実行ファイルをApple Siliconで起動し、Compatibility描画とタイトルのviewportキャプチャを確認。Computer Useが新しいbundle IDを操作対象として認識しなかったため、書き出したアプリの全操作をGUI経由で通す検証は未完了。Godot本体でのゲーム実行とWeb操作は別途確認済み。

ローカルの詳細ログ・キャプチャは`builds/review/`に置く（ビルド成果物としてGit対象外）。`final-checks.txt`、`final-mac-build.txt`、`native-title.png`、`gameplay.png`、`boss.png`。gameplayは通常入力再生、bossは画面確認用の出現時刻fixtureで、gameplay画像内にも検証モードを明記する。

## 残る範囲

物理ゲームパッド、実機スマートフォンのタッチ、Intel Macでの実行、一般配布用の公証は未検証。全章・複数ステージ・キャラ選択・12分版は今回の仕様外。長時間の端末別FPS測定は行っていない。UI操作はキャンバス中心のため、ゲーム内テキストのスクリーンリーダー対応は未実装。

公開デプロイ・GitHubへのpush/mergeはこの作業では行っていない。既存の別アプリのBlenderファイルと共通docs/DESIGN.mdの変更はそのまま保持。

# v0.2 構造遺伝の検証記録

## 自動検証

- Godot 4.7.2 import、Web release、macOS universal export: 成功。`npm run check` に構造テストを組み込み、アプリ差分CIでも毎回実行。
- 既存の遺伝・生態・保存: 9,364 assertions、失敗0。
- 構造: 6,116 assertions、失敗0。両親から各1本の設計を継承、相同器官の発現、親データの非破壊、1,000世代で接続と範囲が有効。4器官すべて・枝分かれ・842個の異なる節配列を確認。842は種数ではない。
- 構造変異7操作、器官の地形効果、不正な循環/節index/小数/NaN/余分なキーを検証。異なる大きさのポートレートに形態が収まり、目の輪郭が重ならないことを検証。
- 保存: 実際のv1コード（main aa1cbbd）で作成した `game/tests/fixtures/legacy-v1.json` を移行。名前、死亡した親、子、14形質、PRNG stateを保持。圧縮保存を再読込した後も250stepの未来が一致。展開サイズ・未知codecの拒否、2万個体分の最大構造が展開上限内であることを検証。
- Linuxの初回CIでseed42が絶滅。構造乱数を生のfloatのhashから小数6桁の整数列hashへ変更し、微小な丸め誤差の増幅を抑制。最終値は修正後の試験結果。OS間の全生態過程の一致は保証しない。
- UI: 122 checks、失敗0。375/768/1024/1440px、375×667の短い画面、祖先の死亡表示・4器官混在・長い世代番号でも本文と系譜ボタンが重ならないことを追加。
- 5 seeds ×15分: 1/7/19/42/20260906。生存31/27/37/34/52、誕生125/128/129/135/142、最大世代12/10/13/11/14。移入・手動雨なし。
- 3 seeds ×1時間: 1/42/20260906。生存42/38/42、誕生594/502/627。15分ごとの継続も確認。器官の維持コストが高かった試作ではseed42が絶滅したため、2組/2節を超える分に各0.001/秒まで調整。
- 200匹開始の200step: 平均約4〜10ms/step（同時実行負荷により変動）（macOS headless、描画を除く）。ブラウザ描画性能の保証ではない。
- 共通 `npm test`: 26 tests成功。CIは対象日フォルダのみ。今回、全アプリのローカルcheckは実行しない。

## 実画面（Computer Use）

- 変更前の通常Webで成体を選択し、既存画面を確認。
- 375×667/375×812/768×812/1024×900/1440×900で島・観察パネルを確認し、スクリーンショットを目視。構造一覧で脚/ヒレ/触手/翼、複数体節、1〜3眼を確認。
- 375pxの親子比較で親2匹と子の姿、継承の説明、親への移動、体節/器官数が読めることを確認。
- 通常Webで以前の島・死亡祖先#254・世代16・誕生302までの系譜を継続。保存成功表示、再読込後の選択個体と世界の継続を確認。
- macOS exportを起動し、新しい形態説明と生物描画を確認。
- 内蔵ブラウザは繰り返し読込・幅変更したタブが一度クラッシュ。新しいタブでは再起動できた。以後の確認は新しいタブを使用し、クラッシュしたタブを検証証拠にしない。原因は未特定で、内蔵ブラウザの長時間安定性は未保証。
- 表示倍率変更後に画面が拡大される不具合を発見。DPRを起動時固定から追従へ修正し、物理解像度が同じままDPRだけ変わるケースもUIテストへ追加。
- 保存形式の移行は実v1 fixtureを用いた自動検証が根拠。ブラウザに保存された生データの形式を直接検査したものではない。

## 限界

器官は4種類の描画規則から派生し、任意の新しい機能が物理法則だけで生まれる仕組みではない。体の接続は片親の設計、器官は両親のスロットから発現する。自由飛行、種の認定、捕食食物網は未実装。全seed・無期限の存続、スマホ200匹の描画速度は保証しない。全滅時の再開導線は維持している。

---

# v0.1の検証記録

## 自動検証

- Godot 4.7.2 import: 構文・型のエラーなし。
- 遺伝/保存/生態: 9,311 assertions、失敗0。交叉で両親から各1 allele、変異の範囲、環境適性、繁殖の両者負担、重複交配、死亡後の系譜、完全な保存復元後の未来一致、破損/不整合/循環/小数indexなどの拒否。
- UI: 101 checks、失敗0。375/768/1024/1440pxの島・観察パネル・操作・モーダルの領域、背景フォーカス遮断と復帰、停止、4倍速、長い名前。
- 5 seeds × 15分の無操作シミュレーション: seed 1/7/19/42/20260906。誕生115/112/130/99/160、生存29/23/28/19/39、最大世代12/12/13/11/12。移入・手動雨なし。
- 200匹開始、200step: macOS headlessで平均約6〜7ms/step (固定0.2秒)。描画時間とは別。端末間の性能保証ではない。

## GUI検証（Computer Use）

- 参照起動: 既存LOOP EATER Webを確認。新規アプリなので変更前の対象画面は存在しない。
- 新規Web: 1440pxで島、個体観察、停止、雨、次の個体、親子比較、子孫からの親参照を操作しスクリーンショット確認。
- 375px縦: 4列2段の操作帯・島・下部観察パネルが収まり、親子ボタンが操作できることを確認。
- 高DPI画面の物理解像度とCSS表示幅の差、デスクトップ観察パネルの幅を修正。viewportごとの機械的領域検証を追加。

- 768pxと1024pxもComputer Useで実画面を確認。375pxの親子比較は説明文・親2匹・子・親への移動ボタン・命名欄が読めることを確認。
- empty: 空の島と手帳からの再開案内を確認。save-error: 自動保存停止と元ファイル退避の案内を確認。qa-load-error: 再試行ボタンを操作し、失敗理由を表示することを確認。
- モーダル内の折返し文が消える問題を修正し、文の最小高さをUIテストに追加。検証用島でのリセット/復旧は通常セーブを書き換えない。
- In-app Browserで長く開いたタブが一度クラッシュ。新しいタブでは正常に起動し、後続の画面確認を実施。ブラウザ長時間連続描画の保証とはしない。
- 3 seeds (1/42/20260906) × 1時間相当の無操作追加監査: 誕生571/507/649、生存40/40/47。15分ごとの生存数も記録。`game/tests/long_run.gd` で再現可能。

- 通常Webで保存成功表示と、再読込後の選択個体/世界の継続を確認。macOS版はexportと起動・描画を確認。30fps化/描画抑制の実験は画面更新の確認が不安定になったため採用せず、元の描画処理を維持。

## 検証範囲の限界

初期版。科学的な種の分化・任意器官の進化・捕食食物網は対象外。Webのスクリーンリーダーにはcanvas説明とキー操作説明を提供するが、全生物の状態をDOMとして公開していない。ネイティブ200匹のモデル負荷と、実機ブラウザの操作確認は別の証拠。長期間すべてのseedが生存する保証はせず、全滅時の系譜と再開導線を用意する。

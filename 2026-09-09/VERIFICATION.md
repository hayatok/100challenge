# Verification — 2026-09-09

## Confirmed

- Godot 4.7.2 import / GDScript解析・型チェック / Web release exportが成功。
- `npm test`（ルート）37件成功。登録アプリ数12件・新規IDを更新。
- `npm run check`（本アプリ）成功。最終の音声再生方式変更後もimport・UI検証・Web buildを再実行。
- コースは347.85m。道路の配置はGodot GUIで作成・保存。再利用カーブの向き・原点もGUIで修正し、上面ビューで全周のつながりを目視確認。
- 順位・3周完走・不正ショートカットの拒否・復帰時の速度クリア・ポーズ・再スタート・タイムアタックでのCPU衝突無効化を検証。
- Physical W入力による加速、ブレーキによる減速、2本指での加速＋旋回、入力クリアをテスト。
- 保存値の型・負数・無限値の拒否を確認。テストの保存先は通常プレイと分離。
- 同じ走行モデルを使う描画ありの自動運転で3周完走。98.867秒、コース外復帰0回。CPUも3台完走。`qa/driving-real-run.png` と `qa/finish-real-run.png` を取得し目視確認。
- 375×812 / 768×1024 / 1024×768 / 1440×900のメニュー・走行・結果レイアウトを描画して確認。`qa/*-375.png`等は状態を設定したレイアウト確認用画像。
- 実ブラウザでも375 / 768 / 1024 / 1440pxを確認。色選択・レース開始・一時停止・再開・コース復帰を操作。

- Webのロード失敗・読み込み中をローカルの検証用URLで確認。
- 最終版を新規タブで起動し、音声を含むブラウザ警告・エラー0件を確認。
- 既存11アプリのローカル成果物を再利用し、新規アプリを加えたショーケースの組み立てが成功。

## Fixed during verification

1. カーブ素材の原点と回転が道路の中心線と違ったため、再利用シーンのメッシュをGUIで調整。
2. 完走済みCPUがゴールを塞いだため、完走後の衝突を解除。
3. 小さい画面でUIが縮みすぎたため、実ビューポートサイズに応じたレイアウトへ変更。
4. 結果表示直後の最小サイズ再計算でパネルが縦に伸びたため、更新後のレイアウトを再計算。
5. Webのデフォルト音声方式では生成ストリームをサンプルとして扱う警告が出たため、ストリーム方式を明示。

6. Retinaの物理解像度でUIが小さくなったため、CSSピクセルに合わせてWebの表示倍率を設定。

## Limits

- 自動運転の完走は操作モデル・経路の成立確認であり、初見の人が楽しく遊べる難易度の証明ではない。
- iPhone実機の同時タッチ・Safari性能、幅375px未満と横向きの低い画面、ゲームパッドは未検証。
- WebGL 2が必要。ロード失敗には再読み込みボタンを用意。
- 公開用のreleaseタグは作成していない。

# Project Instructions

このプロジェクトでは、機能の正しさだけでなく、人間が継続的に使いやすく、画面全体に一貫性があることを重視する。

## Before making changes

作業を始める前に、必ず以下を確認する。

1. リポジトリの構成と現在の実装を確認する
2. `docs/DESIGN.md` を読む
3. `docs/FRONTEND.md` を読む
4. `docs/DAILY_APP_STYLE.md` を読む
5. 関連する画面、コンポーネント、型、テストを確認する
6. 既存のデザインパターンと再利用可能な部品を探す
7. 対象画面を実際に起動し、変更前の状態を確認する

既存コードを読まずに、新しいUIやアーキテクチャを推測して作らない。

## Task definition

すべての作業を、以下の4項目で整理する。

- Goal: 何を変更・追加するのか
- Context: 参照すべきファイル、画面、仕様、エラー
- Constraints: 守るべき設計、技術、UI、データ上の制約
- Done when: 何を満たしたら完了なのか

タスクが複雑、曖昧、複数画面にまたがる場合は、実装前に計画を作る。
重要な前提が不明な場合は、勝手に決めずに質問する。

## Scope control

- 1回の作業では、1つのまとまった目的だけ扱う
- UI改善、機能追加、大規模リファクタリングを混ぜない
- 依頼されていない挙動、ルーティング、データ構造を変更しない
- 必要なファイルだけ変更する
- 既存のコンポーネント、トークン、アイコン、レイアウトを優先して再利用する
- 新しい依存関係を追加する前に、その必要性を説明する
- 既存の命名・設計・責務分担を尊重する
- データの形を推測せず、境界で検証する

## UI development loop

UIを変更するときは、以下の順番で進める。

1. 変更前の画面をブラウザで確認する
2. 対象のルート、画面幅、状態を明確にする
3. 最小限の変更を実装する
4. ブラウザで実際に操作する
5. デスクトップとモバイルで確認する
6. 文字の欠落、重なり、はみ出し、レイアウトの移動を確認する
7. loading、empty、error、successの状態を確認する
8. 必要に応じてスクリーンショットを取得する
9. テスト、lint、型チェック、buildを実行する

UI変更では、コードが動くことだけを完了条件にしない。
実際の画面が読みやすく、操作しやすく、既存画面と一貫していることを確認する。

## Architecture and design consistency

- 毎日のミニアプリは、明示的に異なる方向性が指定されない限り、`docs/DAILY_APP_STYLE.md` のコミカルなビジュアル原則を初期方針とする
- 配色やモチーフはアプリごとに変えてよいが、太い輪郭、フラットな色面、意図のあるズレ、操作に連動する短い演出という共通言語を保つ
- 画面ごとに新しいビジュアル言語を作らない
- `docs/DESIGN.md` にない色、余白、角丸、影、文字サイズをむやみに追加しない
- `docs/FRONTEND.md` のルールに反する実装を行わない
- 既存コンポーネントで表現できるものを新規実装しない
- ページの見た目を整えるためだけに、不要なカードや装飾を追加しない
- 仕様上必要な例外は、理由をコードまたはドキュメントに残す

## Verification

プロジェクトに存在する適切なコマンドを実行する。
以下は例であり、実際のプロジェクトのコマンドを優先する。

```text
[dev command]
[test command]
[lint command]
[typecheck command]
[build command]
```

検証できなかった項目がある場合は、完了報告で明示する。

## Completion report

作業完了時は、以下を簡潔に報告する。

- 変更内容
- 変更したファイル
- 実行したテスト、lint、型チェック、build
- ブラウザで確認した画面と画面幅
- スクリーンショットを確認したか
- 未対応事項、既知の問題、判断した前提

## Continuous improvement

同じ問題や修正指摘が2回以上発生した場合は、単発の修正で終わらせない。

1. なぜ見逃されたかを振り返る
2. `docs/DESIGN.md`、`docs/FRONTEND.md`、`AGENTS.md`のどこにルールを追加すべきか考える
3. 機械的に確認できる場合はlint、テスト、チェックリストにする
4. 同じ問題が再発しないことを確認する

人間のレビューコメントは、可能な限り次回以降も使えるルールへ変換する。

## Work organization

- 毎日作るアプリは、プロジェクトルート直下の `YYYY-MM-DD/` フォルダに分ける
- 日付は、そのアプリの制作を開始した日（Asia/Tokyo）を使う
- アプリ本体、依存関係、README、テストなど、そのアプリ固有のファイルは日付フォルダ内に置く
- `AGENTS.md` と `docs/` は全アプリ共通の設定・ガイドラインとしてルートに置く
- 過去の日付フォルダを、新しいアプリのために流用または上書きしない
- 1つのチャットは1つのまとまった成果物に使う
- 複数の作業を同時に進める場合は、作業単位を分ける
- 同じファイルを並列で変更しない
- 並列作業や実験にはGit worktreeを使う
- 長時間の作業では、途中の判断と進捗を記録する

## Do not

- 「いい感じに」「モダンに」だけを根拠にUIを変更しない
- 既存画面を確認せずにUIを作らない
- 大きな作業を計画なしで開始しない
- 毎回同じ永続ルールをプロンプトに重複して書かない
- テストやブラウザ確認なしで完了扱いにしない
- 依頼されていない大規模な整理や置き換えを行わない
